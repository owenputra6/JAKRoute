# Pembaruan 8 September 2026

## Selesai hari ini

- Menambahkan `palmerah_crowd_areas.geojson` sebagai sumber area crowd.
- Menghasilkan tepat 100 user simulasi dengan ID, posisi, area, lantai, dan bobot sendiri.
- Menghitung density sebagai total bobot / luas sumber; tidak memakai kategori crowd.
- Memakai snapshot yang sama untuk fastest, best-fit, min-walk, API, dan Flutter.
- Menambahkan `GET /crowd/snapshot` serta metadata snapshot pada respons route.
- Menampilkan titik crowd, statistik 9 area, paparan crowd route, dan AI Insight di Flutter.
- Menambahkan provider `station_locations` Supabase di backend.
- Memperbaiki anchor cuaca Palmerah menjadi longitude `106.797...`; warning hanya untuk outdoor.
- Mengunci contoh dependency ke `maplibre_gl` 0.26.2 sesuai build project pengguna.

## Tetap merupakan simulasi

- Network indoor, obstacle, connector, serta koordinat jalur di `station_demo.json`.
- 100 manusia dan bobotnya; GeoJSON hanya menentukan area sampling dan luas.
- Cuaca ketika `WEATHER_MODE=demo`.
- Data lokasi ketika `STATION_DATA_MODE=demo`.

## Membutuhkan konfigurasi pengguna

- `STATION_DATA_MODE=supabase` + URL/key untuk membaca tabel Supabase asli.
- `WEATHER_MODE=google` + Google Weather API key.
- `AGENT_MODE=openai` + OpenAI key/model untuk insight pilihan model.
- MAPID routing contract dan key bila routing outdoor live diaktifkan.
