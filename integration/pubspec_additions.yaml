# Merge with your existing pubspec.yaml, or run:
# flutter pub add http
# flutter pub add maplibre_gl:0.26.2
# No extra Flutter assets are required: catalog/geometry come from Python.
dependencies:
  http: ^1.6.0
  maplibre_gl: 0.26.2
