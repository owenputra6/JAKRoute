# Merge with your existing pubspec.yaml, or run: flutter pub add http maplibre_gl
# No extra Flutter assets are required: catalog/geometry come from Python.
dependencies:
  http: ^1.6.0
  maplibre_gl: ^0.27.0
